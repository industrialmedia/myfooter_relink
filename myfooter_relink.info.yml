name: myfooter_relink
description: My footer relink settings
package: AAMID
type: module
core_version_requirement: ^8 || ^9
version: '1.0.0'

configure: entity.myfooter_relink.collection

dependencies:
  - token
